id: dotfiles/zprofile-local
domain: dotfiles
source: dotfiles/zprofile-local.tmpl
target: ~/.zprofile.local
mode: "0644"
delivery:
  sync: true
  remote: false
safety:
  backup: required
  diff: required
  symlink: reject
  secrets: forbidden
  merge: replace
