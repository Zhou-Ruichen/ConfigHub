id: ai/claude
domain: ai
source: ai/claude/settings.json.tmpl
target: ~/.claude/settings.json
mode: "0600"
delivery:
  sync: true
  remote: false
safety:
  backup: required
  diff: required
  symlink: reject
  secrets: allowed
  merge: managed-section
