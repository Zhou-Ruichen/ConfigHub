# Rendered by ConfigHub for profile {{ .Profile.ID }}. Do not edit by hand;
# rerun `confighub apply` instead. Add only host-specific shell behavior.

# Shared zsh config already adds ~/.local/bin, ~/.npm-global/bin,
# ~/.opencode/bin, and ~/.claude/bin when those directories exist.
# Only add truly host-specific paths and aliases below.

# Example: export PATH="$HOME/.lmstudio/bin:$PATH"
# Example: alias omo='oh-my-opencode-slim'
